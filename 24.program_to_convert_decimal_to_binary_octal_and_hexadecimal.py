# prifix of binary = 0b 
# build in function for binary bin()
# prifix of octal = 0o
# build in function for octal oct()
# prifix of hexadecimal = 0x
# build in function for hexadecimal hex()

# use build in fuctions to convert decimal to above three forms

# take user input of decimal number 

decimal = int(input("Enter a decimal number = "))
print(bin(decimal),"in binary form")
print(oct(decimal),"in octal form")
print(hex(decimal),"in hexadecimal")