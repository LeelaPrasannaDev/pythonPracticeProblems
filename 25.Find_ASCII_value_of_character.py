#ASCII = Amarican standered code for information interchange
# There are 128 characters including numbers, alphabets(upper and lower),symboles,special characters
# build in function of ASCII = ord()

# Take user input to find the value of ASCII

char = input("Enter a charector to find the ASCII value = ")

print('The value of {} in ASCII is = {}'.format(char,ord(char)))